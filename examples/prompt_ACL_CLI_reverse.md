# Prompt to ACL CLI Reverse Engineer

```txt
When installed, i know i can call this program using:

`ACLControlPanel.exe apply`

Is there other param available for the CLI ? Like with options etc
```
